/**
 * @type {import('gatsby').GatsbyConfig}
 */
module.exports = {
  siteMetadata: {
    title: `. Invitato Test`,
    siteUrl: `https://www.yourdomain.tld`,
  },
  plugins: [{ resolve: '@chakra-ui/gatsby-plugin' }],
};
